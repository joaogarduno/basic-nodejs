// Tenemos 2 modulos uno con su definición de la función y en el otro modulo vamos a llamar a esa función
function saludar(nombre){
  return `Hola, ${nombre}`;
}

// Entonces estamos ocupando un modulo en el cual tenga las definiciones y podemos reeusar esa definiciones donde queramos en los otros modulos de la aplicación